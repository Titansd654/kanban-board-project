import { StrikethDirective } from './striketh.directive';

describe('StrikethDirective', () => {
  it('should create an instance', () => {
    const directive = new StrikethDirective();
    expect(directive).toBeTruthy();
  });
});
