import { AppIconColorDirective } from './app-icon-color.directive';

describe('AppIconColorDirective', () => {
  it('should create an instance', () => {
    const directive = new AppIconColorDirective();
    expect(directive).toBeTruthy();
  });
});
